# OS / editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~
.vscode/
.idea/

# Per-user agent tool configs (CLAUDE.md is shared; these dirs are local)
.claude/
.opencode/

# Logs
*.log
logs/

# Env files — secrets never in git
.env
.env.local
.env.*.local

# Credential snapshots pulled back by infrastructure deploy wrappers
.STREAM_KEYS.*

# Node (belt-and-suspenders; streaming-auth/.gitignore also covers these)
node_modules/
dist/
coverage/
.nyc_output/
